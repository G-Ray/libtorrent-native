{
  "name": "libtorrent-native",
  "version": "0.14.0",
  "description": "Rasterbar-libtorrent bindings for Node.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tortank-team/libtorrent-native"
  },
  "license": "MIT",
  "scripts": {
    "install": "node-gyp-build",
    "prebuild": "prebuildify -a --strip",
    "test": "mocha --timeout 60000"
  },
  "devDependencies": {
    "mocha": "^2.2.5",
    "prebuildify": "^2.2.2",
    "segfault-handler": "^1.0.0"
  },
  "dependencies": {
    "nan": "2.4.0",
    "node-gyp-build": "^3.1.0",
    "node-pre-gyp": "0.6.32"
  },
  "main": "./index.js"
}
